[setup]
  providers = ["node"]
  node_version = "20"

[nixpacks.env]
  NODE_ENV = "production"

[phases.build]
  command = "npm install && npm run build"

[start]
  cmd = "npx vite preview --host 0.0.0.0 --port $PORT"
